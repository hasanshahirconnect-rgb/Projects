#include <stdio.h>
int main() {
	int t, x, n, i, j, sum=0;
	scanf("%d", &t);
	for (i = 1; i<=t; i++) {
		scanf("%d", &x);
		scanf("%d", &n);
		for (j=1;j<=n;j++) {
			sum = sum + x;
			x = x * -1;
		}
		printf("%d \n", sum);
		sum = 0;
	}
	return 0;
}
